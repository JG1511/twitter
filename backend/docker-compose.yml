services:
  db:
    image: postgres:latest
    container_name: twitter
    restart: always
    ports:
      - "5435:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=12345678
      - POSTGRES_DB=twitter
    volumes:
      - pgdata:/var/lib/postgresql
volumes:
  pgdata:
